# Linear config for this project

- **Team**: `JO`
- **Default project**: `icruiting` (https://linear.app/tobos/project/icruiting-3c950573e753) — put every issue for either repo in it
- **Repos**: `icruiting-server` (Express/Postgres API, Heroku) and `icruiting-web` (Next.js, Netlify). Prefix issue titles with `server:` / `web:` / `infra:` so it's clear which one a ticket touches
- **Conventions**:
    - The team has no Triage state. New work goes to `Backlog`; promote to `Todo` when it's the next thing to pick up
    - Move to `In Progress` when starting, `Done` when merged
    - No labels exist yet — create them only if a real need shows up, don't invent a taxonomy
